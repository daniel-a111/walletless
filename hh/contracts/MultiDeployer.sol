pragma solidity ^0.8.9;

import "./WalletlessDeployer.sol";


contract MultiDeployer is WalletlessDeployer {

    
    // function deployFor(address[] calldata initializers) external {
    //     for (uint i = 0; i < initializers.length; i++) {
    //         initializers_[new Walletless()] = initializers[i];
    //     }
    // }
}